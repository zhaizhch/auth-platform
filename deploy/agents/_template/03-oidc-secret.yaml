# __AGENT__ 的 OIDC client 凭据（SecurityPolicy.oidc.clientSecret 引用）
# key 必须为 client-secret
apiVersion: v1
kind: Secret
metadata:
  name: eg-__AGENT__-oidc
  namespace: auth-platform
type: Opaque
stringData:
  client-secret: "__CLIENT_SECRET__"
